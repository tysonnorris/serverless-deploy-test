{
  "name": "deploytest",
  "version": "1.0.0",
  "description": "Sample OpenWhisk NodeJS serverless framework service.",
  "main": "handler.js",
  "keywords": [
    "serverless",
    "openwhisk"
  ],
  "scripts": {
    "deploy": "serverless deploy -v",
    "remove": "serverless remove -v",
    "invoke1": "serverless invoke -v --function hello",
    "invoke2": "serverless invoke -v --function hello2",
    "invoke3": "serverless invoke -v --function hello3",
    "invoke4": "serverless invoke -v --function hello4"
  },
  "devDependencies": {
    "serverless-openwhisk": ">=0.13.0"
  }
}
